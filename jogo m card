class Item {
    static getRandomItem() {
        const items = ['banana', 'red_shell', 'green_shell', 'boost'];
        const randomIndex = Math.floor(Math.random() * items.length);
        return items[randomIndex];
    }
}

module.exports = Item;